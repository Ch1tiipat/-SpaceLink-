# SpaceLink Database

> Generated by [`prisma-markdown`](https://github.com/samchon/prisma-markdown)

- [Identity_Organization](#identity_organization)
- [Engagement_Notification](#engagement_notification)
- [Platform_Admin](#platform_admin)
- [Venue_Space](#venue_space)
- [Event](#event)
- [Shop_Category](#shop_category)
- [Booking_Payment](#booking_payment)
- [Refund_Support](#refund_support)

## Identity_Organization

```mermaid
erDiagram
"organization" {
  String organization_id PK
  String name
  String description "nullable"
  String contact_email
  String contact_phone "nullable"
  String facebook_url "nullable"
  String line_url "nullable"
  String promptpay_id "nullable"
  String logo_url "nullable"
  OrgStatus status
  DateTime created_at
  DateTime updated_at
}
"app_user" {
  String user_id PK
  String auth_user_id UK
  String email UK
  String full_name
  String phone "nullable"
  String province "nullable"
  UserRole role
  Int trust_score
  Boolean is_blacklisted
  String blacklist_reason "nullable"
  Json notification_preferences "nullable"
  DateTime created_at
  DateTime updated_at
}
"org_membership" {
  String membership_id PK
  String organization_id FK
  String user_id FK
  MembershipRole role
  Boolean can_edit_quota
  Boolean can_manage_payments
  Boolean can_manage_zones
  DateTime joined_at
}
"org_membership" }o--|| "organization" : organization
"org_membership" }o--|| "app_user" : user
```

### `organization`

Properties as follows:

- `organization_id`:
- `name`:
- `description`:
- `contact_email`:
- `contact_phone`:
- `facebook_url`:
- `line_url`:
- `promptpay_id`:
- `logo_url`:
- `status`:
- `created_at`:
- `updated_at`:

### `app_user`

Properties as follows:

- `user_id`:
- `auth_user_id`:
- `email`:
- `full_name`:
- `phone`:
- `province`:
- `role`:
- `trust_score`:
- `is_blacklisted`:
- `blacklist_reason`:
- `notification_preferences`:
- `created_at`:
- `updated_at`:

### `org_membership`

Properties as follows:

- `membership_id`:
- `organization_id`:
- `user_id`:
- `role`:
- `can_edit_quota`:
- `can_manage_payments`:
- `can_manage_zones`:
- `joined_at`:

## Engagement_Notification

```mermaid
erDiagram
"saved_event" {
  String saved_event_id PK
  String user_id FK
  String event_id FK
  DateTime created_at
}
"push_subscription" {
  String push_subscription_id PK
  String user_id FK
  String endpoint UK
  String p256dh_key
  String auth_key
  String user_agent "nullable"
  DateTime created_at
  DateTime last_used_at "nullable"
}
"review" {
  String review_id PK
  String reviewer_user_id FK "nullable"
  String reviewer_display_name "nullable"
  ReviewTargetType target_type
  String target_id
  Int rating
  String comment "nullable"
  DateTime created_at
  String booking_id FK,UK "nullable"
  String event_id FK "nullable"
  String organization_id FK "nullable"
  ReviewStatus status
}
"notification" {
  String notification_id PK
  String user_id FK
  NotificationType type
  String title
  String body "nullable"
  String related_entity_type "nullable"
  String related_entity_id "nullable"
  Boolean is_read
  DateTime created_at
}
"announcement" {
  String announcement_id PK
  String organization_id FK
  String title
  String body
  Boolean is_active
  DateTime published_at "nullable"
  DateTime created_at
  DateTime updated_at
}
"penalty" {
  String penalty_id PK
  String organization_id FK
  String user_id FK
  String booking_id FK "nullable"
  PenaltyReason reason
  String description "nullable"
  Int points
  DateTime issued_at
  DateTime created_at
}
```

### `saved_event`

Properties as follows:

- `saved_event_id`:
- `user_id`:
- `event_id`:
- `created_at`:

### `push_subscription`

Properties as follows:

- `push_subscription_id`:
- `user_id`:
- `endpoint`:
- `p256dh_key`:
- `auth_key`:
- `user_agent`:
- `created_at`:
- `last_used_at`:

### `review`

Properties as follows:

- `review_id`:
- `reviewer_user_id`:
- `reviewer_display_name`:
- `target_type`:
- `target_id`:
- `rating`:
- `comment`:
- `created_at`:
- `booking_id`:
- `event_id`:
- `organization_id`:
- `status`:

### `notification`

Properties as follows:

- `notification_id`:
- `user_id`:
- `type`:
- `title`:
- `body`:
- `related_entity_type`:
- `related_entity_id`:
- `is_read`:
- `created_at`:

### `announcement`

Properties as follows:

- `announcement_id`:
- `organization_id`:
- `title`:
- `body`:
- `is_active`:
- `published_at`:
- `created_at`:
- `updated_at`:

### `penalty`

Properties as follows:

- `penalty_id`:
- `organization_id`:
- `user_id`:
- `booking_id`:
- `reason`:
- `description`:
- `points`:
- `issued_at`:
- `created_at`:

## Platform_Admin

```mermaid
erDiagram
"system_broadcast" {
  String system_broadcast_id PK
  String title
  String body
  String created_by_user_id FK
  DateTime created_at
  DateTime expires_at "nullable"
}
"audit_log" {
  String audit_log_id PK
  String actor_user_id FK
  String action
  String target_type
  String target_id
  Json metadata "nullable"
  DateTime created_at
}
"org_config" {
  String org_config_id PK
  String organization_id FK,UK
  Int booth_limit_per_vendor "nullable"
  Int booking_quota_per_vendor "nullable"
  Json tier_thresholds "nullable"
  DateTime created_at
  DateTime updated_at
}
"platform_config" {
  String platform_config_id PK
  Decimal(10) base_fee
  Decimal(10) per_zone_rate
  Decimal(10) per_day_rate
  Decimal(10) price_min
  Decimal(10) price_max
  Int default_booking_quota
  Int default_booth_limit "nullable"
  DateTime created_at
  DateTime updated_at
}
"recommendation_log" {
  String recommendation_log_id PK
  String vendor_user_id FK
  String event_id FK
  String recommended_booth_id FK
  RecommendationSource source
  String reason "nullable"
  Decimal(5) score "nullable"
  Boolean was_booked
  DateTime created_at
}
```

### `system_broadcast`

Properties as follows:

- `system_broadcast_id`:
- `title`:
- `body`:
- `created_by_user_id`:
- `created_at`:
- `expires_at`:

### `audit_log`

Properties as follows:

- `audit_log_id`:
- `actor_user_id`:
- `action`:
- `target_type`:
- `target_id`:
- `metadata`:
- `created_at`:

### `org_config`

Properties as follows:

- `org_config_id`:
- `organization_id`:
- `booth_limit_per_vendor`:
- `booking_quota_per_vendor`:
- `tier_thresholds`:
- `created_at`:
- `updated_at`:

### `platform_config`

Properties as follows:

- `platform_config_id`:
- `base_fee`:
- `per_zone_rate`:
- `per_day_rate`:
- `price_min`:
- `price_max`:
- `default_booking_quota`:
- `default_booth_limit`:
- `created_at`:
- `updated_at`:

### `recommendation_log`

Properties as follows:

- `recommendation_log_id`:
- `vendor_user_id`:
- `event_id`:
- `recommended_booth_id`:
- `source`:
- `reason`:
- `score`:
- `was_booked`:
- `created_at`:

## Venue_Space

```mermaid
erDiagram
"venue" {
  String venue_id PK
  String organization_id FK
  String name
  String description "nullable"
  String address "nullable"
  Decimal(9) latitude "nullable"
  Decimal(9) longitude "nullable"
  String google_maps_url "nullable"
  String map_image_url "nullable"
  VenueStatus status
  DateTime created_at
  DateTime updated_at
}
"venue_point" {
  String venue_point_id PK
  String venue_id FK
  VenuePointType type
  String label "nullable"
  Decimal(10) pos_x
  Decimal(10) pos_y
  DateTime created_at
}
"zone" {
  String zone_id PK
  String venue_id FK
  String code
  String name "nullable"
  String description "nullable"
  Decimal(10) default_booth_price "nullable"
  Decimal(10) pos_x "nullable"
  Decimal(10) pos_y "nullable"
  Json image_urls "nullable"
  DateTime created_at
  DateTime updated_at
}
"zone_category" {
  String zone_id FK
  String category_id FK
}
"booth" {
  String booth_id PK
  String zone_id FK
  String code
  Decimal(10) booth_price
  Decimal(6) width_m "nullable"
  Decimal(6) height_m "nullable"
  Json facilities "nullable"
  Decimal(10) pos_x "nullable"
  Decimal(10) pos_y "nullable"
  BoothStatus status
  DateTime created_at
  DateTime updated_at
}
"venue_point" }o--|| "venue" : venue
"zone" }o--|| "venue" : venue
"zone_category" }o--|| "zone" : zone
"booth" }o--|| "zone" : zone
```

### `venue`

Properties as follows:

- `venue_id`:
- `organization_id`:
- `name`:
- `description`:
- `address`:
- `latitude`:
- `longitude`:
- `google_maps_url`:
- `map_image_url`:
- `status`:
- `created_at`:
- `updated_at`:

### `venue_point`

Properties as follows:

- `venue_point_id`:
- `venue_id`:
- `type`:
- `label`:
- `pos_x`:
- `pos_y`:
- `created_at`:

### `zone`

Properties as follows:

- `zone_id`:
- `venue_id`:
- `code`:
- `name`:
- `description`:
- `default_booth_price`:
- `pos_x`:
- `pos_y`:
- `image_urls`:
- `created_at`:
- `updated_at`:

### `zone_category`

Properties as follows:

- `zone_id`:
- `category_id`:

### `booth`

Properties as follows:

- `booth_id`:
- `zone_id`:
- `code`:
- `booth_price`:
- `width_m`:
- `height_m`:
- `facilities`:
- `pos_x`:
- `pos_y`:
- `status`:
- `created_at`:
- `updated_at`:

## Event

```mermaid
erDiagram
"event" {
  String event_id PK
  String organization_id FK
  String venue_id FK
  String name
  String slug UK
  String description "nullable"
  DateTime start_date
  DateTime end_date
  String start_time "nullable"
  String end_time "nullable"
  String banner_url "nullable"
  Json gallery_urls "nullable"
  String map_image_url "nullable"
  String contact_phone "nullable"
  String contact_email "nullable"
  EventStatus status
  DateTime created_at
  DateTime updated_at
}
"event_join_information" {
  String event_join_information_id PK
  String event_id FK
  String title
  String content
  Int sort_order
}
"event_information" {
  String event_information_id PK
  String event_id FK
  String title
  String description
  EventInformationType type
  Int sort_order
}
"event_policy" {
  String event_policy_id PK
  String event_id FK,UK
  String general_rules "nullable"
  String cancellation_policy "nullable"
  String refund_policy "nullable"
  Decimal(5) no_show_deduction_percent
  DateTime created_at
  DateTime updated_at
}
"subscription" {
  String subscription_id PK
  String organization_id FK
  String event_id FK,UK
  SubscriptionStatus status
  Decimal(10) base_fee
  Int zone_count
  Decimal(10) per_zone_rate
  Int event_days
  Decimal(10) per_day_rate
  Decimal(12) calculated_price
  Decimal(10) price_min
  Decimal(10) price_max
  Decimal(12) final_price
  Boolean is_over_max
  DateTime platform_paid_at "nullable"
  DateTime created_at
  DateTime updated_at
}
"event_join_information" }o--|| "event" : event
"event_information" }o--|| "event" : event
"event_policy" |o--|| "event" : event
"subscription" |o--|| "event" : event
```

### `event`

Properties as follows:

- `event_id`:
- `organization_id`:
- `venue_id`:
- `name`:
- `slug`:
- `description`:
- `start_date`:
- `end_date`:
- `start_time`:
- `end_time`:
- `banner_url`:
- `gallery_urls`:
- `map_image_url`:
- `contact_phone`:
- `contact_email`:
- `status`:
- `created_at`:
- `updated_at`:

### `event_join_information`

Properties as follows:

- `event_join_information_id`:
- `event_id`:
- `title`:
- `content`:
- `sort_order`:

### `event_information`

Properties as follows:

- `event_information_id`:
- `event_id`:
- `title`:
- `description`:
- `type`:
- `sort_order`:

### `event_policy`

Properties as follows:

- `event_policy_id`:
- `event_id`:
- `general_rules`:
- `cancellation_policy`:
- `refund_policy`:
- `no_show_deduction_percent`:
- `created_at`:
- `updated_at`:

### `subscription`

Properties as follows:

- `subscription_id`:
- `organization_id`:
- `event_id`:
- `status`:
- `base_fee`:
- `zone_count`:
- `per_zone_rate`:
- `event_days`:
- `per_day_rate`:
- `calculated_price`:
- `price_min`:
- `price_max`:
- `final_price`:
- `is_over_max`:
- `platform_paid_at`:
- `created_at`:
- `updated_at`:

## Shop_Category

```mermaid
erDiagram
"shop" {
  String shop_id PK
  String owner_user_id FK
  String name
  String description "nullable"
  String logo_url "nullable"
  DateTime logo_updated_at "nullable"
  DateTime created_at
  DateTime updated_at
}
"shop_category" {
  String shop_id FK
  String category_id FK
}
"product_category" {
  String category_id PK
  String name UK
  String description "nullable"
  String icon "nullable"
  DateTime created_at
}
"shop_category" }o--|| "shop" : shop
"shop_category" }o--|| "product_category" : category
```

### `shop`

Properties as follows:

- `shop_id`:
- `owner_user_id`:
- `name`:
- `description`:
- `logo_url`:
- `logo_updated_at`:
- `created_at`:
- `updated_at`:

### `shop_category`

Properties as follows:

- `shop_id`:
- `category_id`:

### `product_category`

Properties as follows:

- `category_id`:
- `name`:
- `description`:
- `icon`:
- `created_at`:

## Booking_Payment

```mermaid
erDiagram
"booking_payment_group" {
  String payment_group_id PK
  String payment_code UK
  String vendor_user_id FK
  String shop_id FK
  String event_id FK
  String organization_id FK
  Decimal(12) total_amount
  PaymentGroupStatus status
  DateTime hold_expires_at
  DateTime confirmed_at "nullable"
  DateTime cancelled_at "nullable"
  DateTime created_at
  DateTime updated_at
}
"booking" {
  String booking_id PK
  String booking_code UK
  String payment_group_id FK "nullable"
  String event_id FK
  String booth_id FK
  String shop_id FK
  String vendor_user_id FK
  DateTime booking_start_date
  DateTime booking_end_date
  Decimal(10) booth_price
  Boolean is_payment_exempt
  String payment_exempt_reason "nullable"
  BookingStatus status
  DateTime hold_expires_at "nullable"
  DateTime confirmed_at "nullable"
  String cancelled_by_user_id FK "nullable"
  CancelledByRole cancelled_by_role "nullable"
  String cancel_reason "nullable"
  DateTime cancelled_at "nullable"
  DateTime created_at
  DateTime updated_at
}
"verified_slip" {
  String slip_id PK
  String booking_id FK
  String payment_group_id FK "nullable"
  Decimal(10) amount
  String trans_ref UK "nullable"
  String sending_bank "nullable"
  String sender_name "nullable"
  String receiver_name "nullable"
  String slip_image_url
  SlipStatus slipok_status
  Json slipok_raw "nullable"
  DateTime verified_at "nullable"
  DateTime created_at
}
"booking" }o--o| "booking_payment_group" : paymentGroup
"verified_slip" }o--|| "booking" : booking
"verified_slip" }o--o| "booking_payment_group" : paymentGroup
```

### `booking_payment_group`

Properties as follows:

- `payment_group_id`:
- `payment_code`:
- `vendor_user_id`:
- `shop_id`:
- `event_id`:
- `organization_id`:
- `total_amount`:
- `status`:
- `hold_expires_at`:
- `confirmed_at`:
- `cancelled_at`:
- `created_at`:
- `updated_at`:

### `booking`

Properties as follows:

- `booking_id`:
- `booking_code`:
- `payment_group_id`:
- `event_id`:
- `booth_id`:
- `shop_id`:
- `vendor_user_id`:
- `booking_start_date`:
- `booking_end_date`:
- `booth_price`:
- `is_payment_exempt`:
- `payment_exempt_reason`:
- `status`:
- `hold_expires_at`:
- `confirmed_at`:
- `cancelled_by_user_id`:
- `cancelled_by_role`:
- `cancel_reason`:
- `cancelled_at`:
- `created_at`:
- `updated_at`:

### `verified_slip`

Properties as follows:

- `slip_id`:
- `booking_id`:
- `payment_group_id`:
- `amount`:
- `trans_ref`:
- `sending_bank`:
- `sender_name`:
- `receiver_name`:
- `slip_image_url`:
- `slipok_status`:
- `slipok_raw`:
- `verified_at`:
- `created_at`:

## Refund_Support

```mermaid
erDiagram
"refund_request" {
  String refund_request_id PK
  String booking_id FK
  String requested_by_user_id FK
  String reason
  Decimal(10) requested_amount
  Decimal(10) approved_amount "nullable"
  RefundStatus status
  Json evidence_urls "nullable"
  String payout_method "nullable"
  String payout_prompt_pay_id "nullable"
  String payout_bank_name "nullable"
  String payout_account_number "nullable"
  String payout_account_name "nullable"
  String reviewed_by_user_id FK "nullable"
  DateTime reviewed_at "nullable"
  DateTime processed_at "nullable"
  DateTime created_at
  DateTime updated_at
}
"support_ticket" {
  String support_ticket_id PK
  String user_id FK
  String organization_id FK "nullable"
  String booking_id FK "nullable"
  TicketType type
  String subject
  TicketStatus status
  DateTime created_at
  DateTime updated_at
}
"ticket_message" {
  String ticket_message_id PK
  String ticket_id FK
  String sender_user_id FK
  String message
  Json attachment_urls "nullable"
  DateTime created_at
}
"booth_quota_grant" {
  String quota_grant_id PK
  String vendor_user_id FK
  String event_id FK
  String organization_id FK
  String source_ticket_id FK,UK
  String granted_by_user_id FK
  DateTime granted_at
  String consumed_booking_id FK,UK "nullable"
  DateTime consumed_at "nullable"
}
"ticket_message" }o--|| "support_ticket" : ticket
"booth_quota_grant" |o--|| "support_ticket" : sourceTicket
```

### `refund_request`

Properties as follows:

- `refund_request_id`:
- `booking_id`:
- `requested_by_user_id`:
- `reason`:
- `requested_amount`:
- `approved_amount`:
- `status`:
- `evidence_urls`:
- `payout_method`:
- `payout_prompt_pay_id`:
- `payout_bank_name`:
- `payout_account_number`:
- `payout_account_name`:
- `reviewed_by_user_id`:
- `reviewed_at`:
- `processed_at`:
- `created_at`:
- `updated_at`:

### `support_ticket`

Properties as follows:

- `support_ticket_id`:
- `user_id`:
- `organization_id`:
- `booking_id`:
- `type`:
- `subject`:
- `status`:
- `created_at`:
- `updated_at`:

### `ticket_message`

Properties as follows:

- `ticket_message_id`:
- `ticket_id`:
- `sender_user_id`:
- `message`:
- `attachment_urls`:
- `created_at`:

### `booth_quota_grant`

An approved booth-quota-increase request (SCRUM-182). A row is permission to
exceed the per-event quota by one booking, never a booking itself: the vendor
returns to the normal booking flow and picks an available booth, so approving
cannot beat another vendor to a booth in real time. `consumedBookingId` is
unique, which is what makes spending a grant atomic — two concurrent bookings
cannot both claim it. There is deliberately no `expiresAt`: a grant dies with
its event, because every booking path already refuses a closed event.

Properties as follows:

- `quota_grant_id`:
- `vendor_user_id`:
- `event_id`:
- `organization_id`:
- `source_ticket_id`:
- `granted_by_user_id`:
- `granted_at`:
- `consumed_booking_id`:
- `consumed_at`:
