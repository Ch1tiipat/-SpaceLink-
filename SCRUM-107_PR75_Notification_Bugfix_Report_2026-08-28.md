# SCRUM-107 / PR #75 — Notification Bugfix Report

วันที่ตรวจและแก้ไข: 28 สิงหาคม 2026

ผู้รับผิดชอบ: ซีบิว

Pull Request: [#75 — SCRUM-107: improve user account and notification UX](https://github.com/Ch1tiipat/-SpaceLink-/pull/75)

## สรุปผล

แก้ไขบัคตามรายงาน `SCRUM-107_PR75_Bug_Report_2026-08-27 (1).md` แล้ว โดยจำกัดขอบเขตเฉพาะการแสดงสถานะยังไม่ได้อ่านของ Notification Center ฝั่ง User

ก่อนแก้ไข ระบบซ่อนจุดแจ้งเตือนทันทีเมื่อผู้ใช้เปิดหน้า `/notifications` แม้คำขอทำเครื่องหมายว่าอ่านทั้งหมดที่ส่งไปยัง API จะล้มเหลว ทำให้หน้าจอแสดงว่าผู้ใช้อ่านแจ้งเตือนแล้ว ทั้งที่ข้อมูลในระบบยังไม่เปลี่ยน

หลังแก้ไข ระบบจะซ่อนจุดแจ้งเตือนต่อเมื่อ API `markAllNotificationsRead` ทำงานสำเร็จแล้วเท่านั้น หาก API ล้มเหลว จุดแจ้งเตือนจะไม่ถูกล้างจาก state

## ไฟล์ที่แก้ไข

- `apps/web/components/app-shell.tsx`

## รายละเอียดการแก้ไข

ลำดับเดิม:

1. ตั้งจำนวนแจ้งเตือนที่ยังไม่ได้อ่านเป็น `0`
2. เรียก API เพื่อทำเครื่องหมายว่าอ่านทั้งหมด

ลำดับใหม่:

1. เรียก API เพื่อทำเครื่องหมายว่าอ่านทั้งหมด
2. ตรวจว่า component ยัง active อยู่
3. ตั้งจำนวนแจ้งเตือนที่ยังไม่ได้อ่านเป็น `0`

การตรวจ `active` ช่วยป้องกันการอัปเดต React state หลัง component ถูกถอดออกระหว่างรอ API

## ผลการตรวจสอบ

ตรวจจาก `apps/web` บน branch `codex/user-account-updates-notifications`

| รายการตรวจ | คำสั่ง | ผลลัพธ์ |
|---|---|---|
| ESLint | `npm run lint` | ผ่าน ไม่มี warning หรือ error |
| TypeScript | `npx tsc --noEmit` | ผ่าน Exit code 0 |
| Production build | `npm run build` | ผ่าน สร้างหน้าเว็บครบ 23 routes |
| Diff validation | `git diff --check` | ผ่าน ไม่พบ whitespace error |

## ขอบเขตและความปลอดภัย

- ไม่แก้ไข `apps/api/**`
- ไม่แก้ Prisma schema, migration หรือฐานข้อมูล
- ไม่เปลี่ยน API contract
- ไม่เปลี่ยนระบบสิทธิ์หรือ Supabase configuration
- ไม่รวมงานปรับ UX/UI ของ AI ที่กำลังพัฒนาอยู่
- ไม่แก้ไขไฟล์ Admin หรือฟังก์ชันอื่นนอก SCRUM-107

## ผลกระทบที่คาดหวัง

- สถานะจุดแดงของ Notification Center ตรงกับผลจาก API มากขึ้น
- เมื่อ API ล้มเหลว ผู้ใช้ยังเห็นว่ามีแจ้งเตือนที่ยังไม่ได้อ่าน
- ไม่มีการเปลี่ยนแปลง flow การเข้าสู่ระบบ หน้าแจ้งเตือน หรือข้อมูลการจอง

## ข้อเสนอแนะสำหรับผู้ตรวจ

1. เปิดหน้า User ที่มีแจ้งเตือนยังไม่ได้อ่านและตรวจว่าจุดสีแดงแสดงอยู่
2. เปิดหน้า `/notifications` ในกรณี API สำเร็จและตรวจว่าจุดสีแดงหาย
3. จำลอง API ล้มเหลวและตรวจว่าจุดสีแดงไม่ถูกซ่อนอย่างผิดพลาด
4. ตรวจ CI ของ PR #75 หลัง Push ให้ผ่านครบก่อน Merge
